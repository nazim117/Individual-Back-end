variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

stages:
  - build
  - test

build:
  stage: build
  script:
    - ls
    - pwd
    - ./gradlew assemble

test:
  stage: test
  script:
    - ls
    - pwd
    - ./gradlew test

Job1:
  script:
    - echo "check if works"