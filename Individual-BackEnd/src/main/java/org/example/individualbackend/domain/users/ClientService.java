package org.example.individualbackend.domain.users;

public class ClientService extends User{
    public ClientService(int id, String fName, String lName, String picture, String password) {
        super(id, fName, lName, picture, password);
    }
}
